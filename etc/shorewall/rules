#Forward all ssh and http connection requests from the internet to local system 192.168.1.5
#ACTION         SOURCE  DEST            PROTO   DEST    SOURCE  ORIGINAL
#                                       PORT    PORT(S) DEST
SECTION NEW

Invalid(DROP)   net     all             tcp
Invalid(DROP)   vr      all             tcp
SSH(ACCEPT)     net     $FW
SSH(ACCEPT)     vr      $FW
Ping(ACCEPT)    vr      $FW
Ping(ACCEPT)    net     $FW
HTTP(ACCEPT)    net     $FW
HTTP(ACCEPT)    vr      $FW
ACCEPT          vr      $FW             tcp 8080
ACCEPT          net     $FW             tcp 8080
ACCEPT          $FW     net
ACCEPT          $FW     vr
DNS(ACCEPT)     vr      $FW
DNS(ACCEPT)     net     $FW
ACCEPT          vr      $FW             tcp 5201 #iperf3
ACCEPT          vr      $FW             udp 5201
DNAT            net     loc:192.168.1.3 tcp 5646 #retroshare
DNAT            net     loc:192.168.1.3 udp 5646
